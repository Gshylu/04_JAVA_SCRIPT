  /*  var array1 =[1,2,3,4,5];
    var array2 =[1,2,3,5,4,5];

    const check = array1.length == array2.length &&
     array1.every((curElement)=>{
        if(array2.indexOf(curElement) > -1){
            return(curElement =array2[array2.indexOf(curElement)])
        }
     });*/

     //indexof() returns -1 if the value is not found then the value will be flase